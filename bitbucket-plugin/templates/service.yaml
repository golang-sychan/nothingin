apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitbucket-plugin.fullname" . }}
  labels:
    {{- include "bitbucket-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitbucket-plugin.selectorLabels" . | nindent 4 }}
